name: client
description: "A Sketch quiz app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0-207.0.dev
dependencies:
  flutter:
    sdk: flutter
  go_router:
  google_fonts:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true